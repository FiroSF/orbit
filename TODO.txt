스레딩?
  js는 기본적으로 싱글스레딩이지만, worker를 활용하면 멀티스레딩이 가능함
  ts의 경우, js의 worker를 그대로 쓸 수 없는 것으로 보임.
  그래서 https://www.npmjs.com/package/ts-worker 이런거 써야 할 듯.
  개체마다 하나의 worker를 가지고, 렌더링할때 worker들을 한번에 실행시키는건 어떨까
  이걸 하기 위해서는 worker들을 모두 실행시켰을 때 실행하는 걸 구현해야 함
  이것도 이벤트로 이리쿵저리쿵하면 될거같은데..?
리얼타임체커
프레임 계수기
애니메이션
  애니메이션 개선이 필요함 / 리소스를 너무 많이 잡아먹음
발사 예상궤적
캔버스 움직임
movingInvertedPlanet
일정이상 넘어가면 지우는거
git 브랜치좀 체계적으로 
계산방식 변경
  지금은 setInterval로 작동하는데, setInterval은 고정된 실행주기를 보장하지 않음
  차라리 프레임제한을 두지 않는 방식으로 만들어 보고, 프레임제한은 그냥 저번 작동시간을 계산해서
  남은시간을 sleep같은걸 찾아서쓸순없나? 근데 저번에 내가 sleep 있나 찾아봤을때 없었던것같기도하구
